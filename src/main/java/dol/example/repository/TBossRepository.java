package dol.example.repository;

import dol.example.domain.TBoss;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TBossRepository extends JpaRepository<TBoss, Integer> {
}
